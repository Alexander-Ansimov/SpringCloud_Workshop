spring.application.name: HystrixDashboard

server.port: 8070

eureka:
 client:
   fetchRegistry: true
   registerWithEureka: true
   serviceUrl:
     defaultZone: http://localhost:8761/eureka

turbine:
  appConfig: Shop,User,Warehouse
  clusterNameExpression: new String("default")